# Plaintext data — NEVER commit (only the encrypted *.enc.json files are public)
data.json
*.xlsx
*.xls

# Secrets
.github_token
*.token

# Generated offline build (contains embedded encrypted data; regenerated by sync)
dashboard-local.html

# Local dev tooling that may embed passwords/wordlists
tools/reencrypt.js

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.gstack/
